Convert the following CSV data into a JSON array. Infer appropriate types: numbers should be JSON numbers, booleans should be JSON booleans, dates should be ISO 8601 strings, and everything else should be strings.

Respond with ONLY the JSON array, nothing else.

CSV data:
{{.Input}}