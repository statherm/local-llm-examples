You are an assistant extracting action items from meeting notes. Given the following meeting notes, extract all action items as a JSON array. Each action item should have these fields:
- "owner": the person responsible (string)
- "action": what they need to do (string)
- "deadline": when it's due, or null if not specified (string or null)

Respond with ONLY the JSON array, nothing else.

Meeting notes:
{{.Input}}