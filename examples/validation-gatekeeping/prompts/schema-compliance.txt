You are a data validation assistant. Check whether the following JSON document complies with the described schema rules. Identify any violations.

Respond with a JSON object containing:
- "valid": boolean indicating overall compliance
- "issues": an array of objects, each with "field" (string) and "problem" (string)

If the document is valid, "issues" should be an empty array.

Respond with ONLY the JSON object, nothing else.

Schema rules:
{{.Schema}}

JSON document:
{{.Input}}